
XY=loadlast(unitposifile,2);
CePo=loadlast(cellcenterposifile,2);
As=loadlast(cellareafile,1);
Ps=loadlast(cellperimeterfile,1);
Es=loadlast(cellenergyfile,1);
totenergy=sum(Es);
pArea=loadlast(unitareafile,1);
pPerimeter=loadlast(unitperimeterfile,1);